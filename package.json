{
  "name": "insert_many_documents_to_mongodb_collection_from_js_array",
  "version": "1.0.0",
  "description": "a script for inserting many documents from an array of objects directly to mongodb database collection",
  "main": "script.js",
  "scripts": {
    "start": "node script.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevin Ariza",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "mongodb": "^4.3.1"
  }
}
